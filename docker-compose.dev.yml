version: '3.8'

services:
  app:
    build:
      target: builder
    volumes:
      - .:/app
      - go-modules:/go/pkg/mod
    environment:
      ENVIRONMENT: development
      LOG_LEVEL: debug
    command: go run cmd/api/main.go

volumes:
  go-modules: